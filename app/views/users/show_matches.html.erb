<section>
  <%= image_tag @user.avatar.url(:medium) %>
  <h2><%= @user.name %></h1>
</section>

<section>
  <h3><%= render 'shared/match_stats' %></h3>
</section>

<% if @users.any? %>
  <ul class="users">
    <%= render @users %>
  </ul>
<% end %>
