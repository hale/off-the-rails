<%= form_for User.find(session[:user_id]).relationships.find_by_match_id(user),
             :html => { :method => :delete } do |f| %>
  <%= f.button "Remove", :class => 'btn btn-danger btn-mini' %>
<% end %>