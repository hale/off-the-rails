<%= form_for User.new, :url => {:action => 'search',:id => session[:user_id],  :controller => 'users'}, :html => {:class => 'navbar-search pull-right'} do |f| %>
	<%= f.text_field :name, :placeholder => 'Search', :class => 'search-query' %>
<% end %>