<div class="container">

  <div class="sidebar">
    <!-- This will presently just display an error.  Full messages would necessitate moving the 'create' action from the users to the pages controller.  I figured handling input errors is best done with javascript anyway.  If we do decide to move the create action, the form_for helper under shared/sign_up will need a new option ':url => pages_path' -->
      <% flash.each do |key,value| %>
        <div class="flash <%= key %>"><%= value %> </div>
      <% end -%>
    <div class="signup">
      <%= render 'shared/sign_up' %> 
      <h3>Log in</h3>
        <p>render 'shared/log_in'</p>
    </div>


    <div class="footer">
      <%= link_to "about", about_path %>
       / 
      <%= link_to "help", help_path %>
    </div>
  </div>

  <div class="main">
    


    <div class = "banner"><%= link_to banner, root_path %></div>
    <div class="information">
      <div class="info_box">
        Sample Image 1
      </div>
      <div class="info_box">
        Sample Image 2
      </div>
      <div class="info_box">
        Sample Image 3
      </div>
    </div>

  </div>

  </div>

</div>
